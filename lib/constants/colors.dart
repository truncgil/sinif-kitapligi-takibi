import 'package:flutter/material.dart';

/// Uygulama genelinde kullanılan renkler
class AppColors {
  /// Ana tema rengi
  static const Color primary = Color(0xFF04BF61);
  static const Color secondary = Color.fromARGB(255, 77, 212, 145);

  /// Koyu tema için arka plan rengi
  static const Color darkBackground = Color(0xFF121212);

  /// Koyu tema için yüzey rengi
  static const Color darkSurface = Color(0xFF1E1E1E);

  /// Koyu tema için kart rengi
  static const Color darkCard = Color(0xFF2D2D2D);

  /// Menü ikon ve header renkleri
  static const Color borrow = Color.fromARGB(255, 95, 209, 152); // Yeşil
  static const Color classRoom = Color.fromARGB(255, 148, 109, 255); // Mor
  static const Color student = Color(0xFFFF9800); // Turuncu
  static const Color book = Color.fromARGB(255, 98, 184, 255); // Mavi
  static const Color history = Color.fromARGB(255, 225, 151, 124); // Kahverengi
  static const Color statistics = Color.fromARGB(255, 237, 134, 132); // Kırmızı
  static const Color backup = Color.fromARGB(255, 129, 175, 198); // Gri
}
